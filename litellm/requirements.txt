# Runtime dependencies for the governance enforcement layer.
# Pin exact versions in your own lockfile/deployment; these floors are the tested minimums.
# Refresh with: pip install -U "litellm[proxy]" and update the floor below.

litellm[proxy]>=1.55.0

# Dev/test only (install with: pip install -r requirements-dev.txt if you split them).
pytest>=8.0
pytest-asyncio>=0.23
